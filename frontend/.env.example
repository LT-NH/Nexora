# Nexora 前端环境变量示例（复制为 .env.local 并按需填写）

# 错误监控（Sentry）——留空则不启用
# VITE_SENTRY_DSN=https://xxx@o000000.ingest.sentry.io/0000000

# 静态托管模式（花生壳 Drop 等无 SPA fallback 的服务器设为 1，启用 HashRouter）
# VITE_STATIC_HOST=1

# 后端 API 基址（默认同源 /api 代理，无需配置）
# VITE_API_BASE_URL=http://127.0.0.1:8000
