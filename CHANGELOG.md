# Changelog

本文件记录 Nexora 的所有重要变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [5.4.0] - 2026-09-13

### Added
- **自主巡店 Agent（Store Sentinel）**：每日自主当班——感知店铺真实状态 → 大模型自主决策 → 分级处理（低风险动作自主执行 / 改价建券等破坏性动作挂起请店主确认）→ 审计落库 → 结论通知 → 经验沉淀。
- **Agent 自动回访闭环**：巡店时自动扫描“已执行但未验证”的动作，对比执行基线（`result_before`）与当前经营指标，自动判定命中 / 未命中并生成教训写入经验库——让“越用越聪明”成为可验证事实而非口号。
- **单品毛利归因榜**：`GET /ai/profit-by-product`——基于真实订单行与商品成本价归因每个 SKU 的营收 / 成本 / 毛利 / 毛利率，输出盈利 Top 与亏损 Bottom 榜及经营建议；未录入成本价的 SKU 单独计数（不伪造毛利率）。
- **支付宝支付通道**：接入 `alipay.trade.page.pay`（电脑网站支付 / AI 网页应用收款）——RSA2 服务端自实现下单表单、异步通知验签、订单激活全链路；凭据齐备即真实收款。
- **Sentry 错误监控**：前后端统一接入，`SENTRY_DSN` / `VITE_SENTRY_DSN` 留空时静默跳过（本地开发零副作用）；启用后自动上报异常与性能追踪。
- **运营管理 tab**：新增「账户与订阅」卡（订阅状态 / 剩余天数 / 当前套餐 / 团队与密钥入口）。

### Changed
- **工作台信息架构重排**：经营健康引擎（六维雷达）升为概览第一屏焦点；AI 决策助手 / 巡店 Agent / 经营 KPI 归入概览 tab；库存预警归入运营 tab；周报统计归入数据洞察 tab——切换键真正控制内容。
- **切换键吸顶**：概览 / 数据洞察 / 运营管理常驻顶栏下方，滚动全程可切换。
- **套餐能力矩阵规范化**：Free（健康体检）/ Pro（+AI 决策助手、经验库、利润归因）/ Enterprise（+自主巡店 Agent）三档边界明确，后端端点级强制 + 前端锁卡引导。
- **视觉语言统一**：移除装饰性彩色边框与渐变，卡片统一为白底 + 细描边 + 统一内边距；语义色仅用于图标与状态。

### Fixed
- **订阅换购失效**：激活逻辑改为每个工作空间单条订阅（换购即更新），修复同秒多条订阅导致的状态判定混乱。
- **档位判定被历史数据压低**：档位查询改为取生效中（ACTIVE）订阅的**最高档**，修复 Enterprise 用户被误判为 Pro 而 403 拦截。
- **新手引导浮层遮挡支付弹窗**：引导浮层 z-index 由 100 降至 40，避免遮挡 Modal 内的支付操作。
- **Free 档越权使用 AI 能力**：15 个 AI 端点补齐套餐门控（Free 仅保留健康体检）。
- 前端切换键吸顶被顶栏遮挡（sticky 偏移修正）。
